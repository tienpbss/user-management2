const formSubmitStatus = require('./form-submit-status');
const formStatus = require('./form-status');
const roles = require('./roles');
const formCategory = require('./form-category');


module.exports = {
    formStatus,
    formSubmitStatus,
    roles,
    formCategory
}